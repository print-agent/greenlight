# greenlight

API Endpoints Practice

### working with environement variables

- external .env file to abstract critical info from users (must be added to .gitignore)
- load_env.ps1 script to import the environement variables from .env file into the current-shell-session
-
